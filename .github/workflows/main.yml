name: Deploy to Fly.io

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to Fly.io
        run: flyctl login --token ${{ secrets.FLY_API_TOKEN }}
      - name: Deploy
        run: flyctl deploy
